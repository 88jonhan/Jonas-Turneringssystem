/* class MenuConstructor
{
    public MenuConstructor(IEnumerable<IMenuItem> menuItems, string action)
    {
        List<MenuItem> MenuItems = new();
        menuItems.ToList().ForEach(item => MenuItems.Add(new MenuItem(item, action)));

        Menu menu = new Menu(MenuItems);
    }
} */